*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Dumps de base de datos: contienen datos de cliente + hashes de credenciales. NUNCA versionar.
docs/*.sql

# Material de presentación / notas internas de demo
docs/presentacion/GUION-DEMO.md
docs/comparacion-tipo4.md

# Artefactos generados de demo y presentación: speeches privados, pptx, capturas del
# sistema en vivo y renders. Son binarios pesados y material interno. NUNCA versionar:
# una vez en el historial no se sacan más, y cada clon se los lleva.
/outputs

# SDD skill-registry cache (regenerable, not an SDD artifact)
/.atl
